<div class="container">

<% header = @user.username %>

<div class="card text-center ml-auto mr-auto mb-5" style="width: 30rem;">
	  <img class="card-img-top" alt="image pin" src=<%= @post.img_url %> >
<div class=“card-body”>
 <h5 class=“card-title”><%= @post.title %></h5>
 <h5 class=“card-title”><%= @post.description %></h5>
 <h5 class=“card-title”><%= @post.category.cusines %></h5>
</div>
</div>

<div class="text-center mb-5">
		<%= link_to 'Back', root_path, {:class => "btn btn-outline-secondary"} %>

		<%= link_to 'Edit Post', edit_post_path(@post), {:class => "btn btn-outline-primary"} %>
	</div>

</div>